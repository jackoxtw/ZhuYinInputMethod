# 逐音輸入法：一般使用者安裝說明

這份說明適用於已下載 Release 的使用者。**不需要安裝 Xcode，也不需要自行編譯。**

若專案已啟用 GitHub Pages，可先使用 [HTML 線上試用版](https://jackoxtw.github.io/ZhuYinInputMethod/) 體驗操作；它是瀏覽器原型，不會安裝輸入法，也不會取代以下 macOS 安裝步驟。

## 下載 Release

[直接下載逐音輸入法 v0.1.49 ZIP](https://github.com/jackoxtw/ZhuYinInputMethod/releases/download/v0.1.49/ZhuYinInputMethod-v0.1.49.zip)

也可瀏覽 [v0.1.49 發布頁](https://github.com/jackoxtw/ZhuYinInputMethod/releases/tag/v0.1.49)。

下載 ZIP 後請完整解壓縮；資料夾內包含 `逐音輸入法.app`、`安裝逐音輸入法.command` 與 `說明.txt`。

## 系統需求

- macOS 12 Monterey 或更新版本。
- Apple Silicon Mac（M1、M2、M3、M4 或更新機型）。
- 可輸入管理員密碼的帳號。

## 安裝步驟

1. 從 GitHub Releases 下載並解壓縮 `逐音輸入法-v*.zip`。
2. 保留整個解壓縮後的資料夾，不要單獨移動裡面的檔案。
3. 以滑鼠右鍵點擊 `安裝逐音輸入法.command`，選擇「打開」。
4. 若 macOS 顯示無法驗證開發者，請到「系統設定 → 隱私權與安全性」，按「仍要打開」，再重新執行安裝器。
5. 終端機會要求輸入 Mac 管理員密碼。輸入時游標不會顯示字元，這是正常的；輸入完成後按 Enter。
6. 安裝器完成後會開啟鍵盤設定。請依序選擇「鍵盤 → 文字輸入 → 編輯 → + → 繁體中文 → 逐音輸入法 → 加入」。

## 使用方式

- 從選單列的輸入法選單切換到「逐音輸入法」。
- 輸入注音後，使用方向鍵選擇候選；按 Enter 或 Space 確認。
- 按 `F9` 可切換繁體／簡體最終輸出。
- 按反引號 `` ` `` 開啟 Emoji 候選；按單引號 `'` 開啟常用中文標點。
- 按住 Option 時，曾學習過的候選右上角會顯示「刪除」；點選即可移除該候選的學習排序資料。
- 單按 `Shift` 切英文時，未確認的注音會直接取消；已確認的中文會先送出。輸入中文時按 `Shift + 英文字母` 也採相同規則，再輸入英文。

HTML 試用版同樣支援自適應候選面板、右側「繁／簡／說明／清除學習」控制列與 Option 刪除已學習候選；其學習資料只存在目前瀏覽器。

## 常見問題

### 找不到「逐音輸入法」

關閉並重新開啟「系統設定 → 鍵盤 → 文字輸入 → 編輯」，再按 `+` 搜尋「逐音輸入法」。

### macOS 阻擋安裝器或輸入法

Release 是 ad-hoc 簽章，未使用 Apple Developer ID 或 notarization。請確認 Release 來源可信後，在「系統設定 → 隱私權與安全性」按「仍要打開」。這是每台 Mac 第一次執行時可能需要完成的一次性安全確認。

### 安裝器要求密碼

輸入法會安裝到系統的 `/Library/Input Methods/`，因此需要 Mac 管理員密碼。密碼不會被安裝器保存或傳送。

### Intel Mac

目前 Release 僅提供 Apple Silicon（arm64）版本；Intel Mac 需要另一個 x86_64 或 universal 版本。
